Hello fellow user, in the triangle.py, is a code where the user inputs values for the triangle and then calculated too see if the values from the input are valid to be considerd as an triangle. In here I leanred many things when it comes to coding python like valueError, which is when the code fails it will execute the code that is programed in  it.
